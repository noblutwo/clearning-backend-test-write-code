# 🏗️ Project Architecture Diagram

## Complete Application Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    HTTP REQUEST                              │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │     ELYSIA HTTP HANDLER            │
        │  src/index.ts + Route Registration │
        └────────────────┬───────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │        MIDDLEWARE CHAIN            │
        │  - Logger (logs all requests)      │
        │  - CORS (handles cross-origin)     │
        │  - Error Handler (catches errors)  │
        └────────────────┬───────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │      ROUTES (7-layer split)        │
        │  /api/v1/users  → UserController  │
        │  /api/v1/posts  → PostController  │
        │  /api/v1/health → HealthCheck     │
        └────────────────┬───────────────────┘
                         │
        ┌────────────────┴────────────────┐
        │                                 │
        ▼                                 ▼
┌───────────────────┐           ┌───────────────────┐
│  USER CONTROLLER  │           │  POST CONTROLLER  │
│  - getAll()       │           │  - getAll()       │
│  - getById()      │           │  - getById()      │
│  - create()       │           │  - getBySlug()    │
│  - update()       │           │  - create()       │
│  - delete()       │           │  - update()       │
└────────┬──────────┘           │  - publish()      │
         │                      │  - delete()       │
         │                      └────────┬──────────┘
         │                              │
         ▼                              ▼
┌───────────────────┐           ┌───────────────────┐
│  USER SERVICE     │           │  POST SERVICE     │
│  - getAllUsers()  │           │  - getAllPosts()  │
│  - getUserById()  │           │  - getPostById()  │
│  - createUser()   │           │  - getPostBySlug()│
│  - updateUser()   │           │  - getPostsByStatus
│  - deleteUser()   │           │  - createPost()   │
│                   │           │  - updatePost()   │
│  Validation &     │           │  - publishPost()  │
│  Business Logic   │           │  - deletePost()   │
└────────┬──────────┘           │                   │
         │                      │  + Slug uniqueness│
         │                      │  + View counting  │
         │                      │  + Workflow mgmt  │
         │                      └────────┬──────────┘
         │                              │
         ▼                              ▼
┌───────────────────┐           ┌───────────────────┐
│  USER REPOSITORY  │           │  POST REPOSITORY  │
│  - findAll()      │           │  - findAll()      │
│  - findById()     │           │  - findById()     │
│  - findByEmail()  │           │  - findBySlug()   │
│  - create()       │           │  - findByStatus() │
│  - update()       │           │  - create()       │
│  - softDelete()   │           │  - update()       │
│  - hardDelete()   │           │  - incrementView()│
│                   │           │  - softDelete()   │
│  (QueryBuilder)   │           │  - hardDelete()   │
└────────┬──────────┘           │                   │
         │                      │  (QueryBuilder +  │
         │                      │   advanced ops)   │
         │                      └────────┬──────────┘
         │                              │
         └──────────────┬───────────────┘
                        │
                        ▼
        ┌────────────────────────────────────┐
        │     TYPEORM REPOSITORIES           │
        │  - Entity Definitions              │
        │  - Column Mappings                 │
        │  - Relationship Management         │
        └────────────────┬───────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │     DATABASE CONNECTION            │
        │  - PostgreSQL Driver               │
        │  - Connection Pooling              │
        │  - Transaction Management          │
        └────────────────┬───────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │     POSTGRESQL DATABASE            │
        │  - users table                     │
        │  - posts table                     │
        │  - Indices (slug, status, etc)     │
        │  - Soft delete (deletedAt column)  │
        └────────────────────────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │     HTTP RESPONSE                  │
        │  {                                 │
        │    status: 200,                    │
        │    data: { ... },                  │
        │    timestamp: "2024-01-01T..."     │
        │  }                                 │
        └────────────────────────────────────┘
```

---

## Module Architecture (Detailed View)

```
┌─────────────────────────────────────────────────────────────────┐
│                     USER MODULE                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  ROUTES: src/routes/v1/users.routes.ts                  │  │
│  │  - GET /api/v1/users                                    │  │
│  │  - GET /api/v1/users/:id                                │  │
│  │  - POST /api/v1/users                                   │  │
│  │  - PUT /api/v1/users/:id                                │  │
│  │  - DELETE /api/v1/users/:id                             │  │
│  └──────────┬───────────────────────────────────────────────┘  │
│             │                                                   │
│  ┌──────────▼───────────────────────────────────────────────┐  │
│  │  CONTROLLER: src/controllers/user.controller.ts         │  │
│  │  - Handles HTTP requests/responses                      │  │
│  │  - Input validation (DTO check)                         │  │
│  │  - Status code mapping                                  │  │
│  └──────────┬───────────────────────────────────────────────┘  │
│             │                                                   │
│  ┌──────────▼───────────────────────────────────────────────┐  │
│  │  SERVICE: src/services/user.service.ts                  │  │
│  │  - getAllUsers()                                        │  │
│  │  - getUserById()                                        │  │
│  │  - createUser() - Validation & Error handling           │  │
│  │  - updateUser() - Business logic                        │  │
│  │  - deleteUser() - Soft delete implementation            │  │
│  └──────────┬───────────────────────────────────────────────┘  │
│             │                                                   │
│  ┌──────────▼───────────────────────────────────────────────┐  │
│  │  DTO: src/dtos/user.dto.ts                              │  │
│  │  - CreateUserDTO (type-safe input)                      │  │
│  │  - UpdateUserDTO                                        │  │
│  │  - UserResponseDTO (API response shape)                 │  │
│  └──────────┬───────────────────────────────────────────────┘  │
│             │                                                   │
│  ┌──────────▼───────────────────────────────────────────────┐  │
│  │  REPOSITORY: src/repositories/user.repository.ts        │  │
│  │  - findAll() - WHERE deletedAt IS NULL                  │  │
│  │  - findById() - UUID lookup                             │  │
│  │  - findByEmail() - Email search                         │  │
│  │  - create() - INSERT with UUID generation               │  │
│  │  - update() - UPDATE with timestamp                     │  │
│  │  - softDelete() - SET deletedAt = NOW()                 │  │
│  │  - hardDelete() - DELETE FROM database                  │  │
│  └──────────┬───────────────────────────────────────────────┘  │
│             │                                                   │
│  ┌──────────▼───────────────────────────────────────────────┐  │
│  │  ENTITY: src/database/entities/user.entity.ts           │  │
│  │  @Entity('users')                                       │  │
│  │  - id: UUID (PrimaryGeneratedColumn)                    │  │
│  │  - name: varchar(255)                                   │  │
│  │  - email: varchar(255, unique)                          │  │
│  │  - phone: varchar(20, nullable)                         │  │
│  │  - address: varchar(500, nullable)                      │  │
│  │  - status: varchar(50, default='active')                │  │
│  │  - createdAt: timestamp (auto)                          │  │
│  │  - updatedAt: timestamp (auto)                          │  │
│  │  - deletedAt: timestamp (nullable)                      │  │
│  └──────────┬───────────────────────────────────────────────┘  │
│             │                                                   │
│  ┌──────────▼───────────────────────────────────────────────┐  │
│  │  DATABASE: PostgreSQL Table                             │  │
│  │  Table: users (with migrations)                         │  │
│  │  Indices: email, status, deletedAt                      │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                     POST MODULE (EXTENDED)                       │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  ROUTES: src/routes/v1/posts.routes.ts                  │  │
│  │  - GET /api/v1/posts                                    │  │
│  │  - GET /api/v1/posts/:id                                │  │
│  │  - GET /api/v1/posts/slug/:slug        [NEW]            │  │
│  │  - POST /api/v1/posts                                   │  │
│  │  - PUT /api/v1/posts/:id                                │  │
│  │  - PATCH /api/v1/posts/:id/publish    [NEW]             │  │
│  │  - DELETE /api/v1/posts/:id                             │  │
│  └──────────┬───────────────────────────────────────────────┘  │
│             │                                                   │
│  ┌──────────▼───────────────────────────────────────────────┐  │
│  │  CONTROLLER: src/controllers/post.controller.ts         │  │
│  │  - getAll() - Return published posts                    │  │
│  │  - getById() - Fetch single post, increment views       │  │
│  │  - getBySlug() - Slug-based lookup [NEW]                │  │
│  │  - create() - Validate slug uniqueness [NEW]            │  │
│  │  - update() - Conflict detection                        │  │
│  │  - publish() - Change status action [NEW]               │  │
│  │  - delete() - Soft delete with audit                    │  │
│  └──────────┬───────────────────────────────────────────────┘  │
│             │                                                   │
│  ┌──────────▼───────────────────────────────────────────────┐  │
│  │  SERVICE: src/services/post.service.ts                  │  │
│  │  - getAllPosts()                                        │  │
│  │  - getPostById() - Auto-increment viewCount [NEW]       │  │
│  │  - getPostBySlug() - Slug validation [NEW]              │  │
│  │  - getPostsByStatus() - Filter workflow [NEW]           │  │
│  │  - createPost() - Check slug uniqueness [NEW]           │  │
│  │  - updatePost() - Conflict resolution                   │  │
│  │  - publishPost() - Status change [NEW]                  │  │
│  │  - deletePost() - Soft delete workflow                  │  │
│  └──────────┬───────────────────────────────────────────────┘  │
│             │                                                   │
│  ┌──────────▼───────────────────────────────────────────────┐  │
│  │  DTO: src/dtos/post.dto.ts                              │  │
│  │  - CreatePostDTO                                        │  │
│  │  - UpdatePostDTO                                        │  │
│  │  - PostResponseDTO                                      │  │
│  └──────────┬───────────────────────────────────────────────┘  │
│             │                                                   │
│  ┌──────────▼───────────────────────────────────────────────┐  │
│  │  REPOSITORY: src/repositories/post.repository.ts        │  │
│  │  - findAll() - WHERE status='published' AND deleted     │  │
│  │  - findById() - UUID lookup                             │  │
│  │  - findBySlug() - Unique slug lookup [NEW]              │  │
│  │  - findByStatus() - Status filtering [NEW]              │  │
│  │  - create() - INSERT with timestamps                    │  │
│  │  - update() - UPDATE with version check                 │  │
│  │  - incrementViewCount() - Atomic increment [NEW]        │  │
│  │  - softDelete() - SET deletedAt = NOW()                 │  │
│  │  - hardDelete() - DELETE FROM database                  │  │
│  └──────────┬───────────────────────────────────────────────┘  │
│             │                                                   │
│  ┌──────────▼───────────────────────────────────────────────┐  │
│  │  ENTITY: src/database/entities/post.entity.ts           │  │
│  │  @Entity('posts')                                       │  │
│  │  - id: UUID (PrimaryGeneratedColumn)                    │  │
│  │  - title: varchar(255)                                  │  │
│  │  - slug: varchar(255, unique) [NEW]                     │  │
│  │  - content: text                                        │  │
│  │  - description: varchar(500, nullable)                  │  │
│  │  - status: varchar(50, default='draft') [NEW]           │  │
│  │  - viewCount: int, default=0 [NEW]                      │  │
│  │  - createdAt: timestamp (auto)                          │  │
│  │  - updatedAt: timestamp (auto)                          │  │
│  │  - deletedAt: timestamp (nullable)                      │  │
│  └──────────┬───────────────────────────────────────────────┘  │
│             │                                                   │
│  ┌──────────▼───────────────────────────────────────────────┐  │
│  │  DATABASE: PostgreSQL Table                             │  │
│  │  Table: posts (with migrations)                         │  │
│  │  Indices: slug [UNIQUE], status, createdAt [NEW]        │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## Type Safety Flow

```
┌─────────────────────────────────────────────────────────┐
│          CLIENT REQUEST (JSON)                          │
│  POST /api/v1/posts                                     │
│  {                                                      │
│    "title": "My Post",                                  │
│    "slug": "my-post",                                   │
│    "content": "Content..."                              │
│  }                                                      │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│    ELYSIA VALIDATOR (Route Type Check)                  │
│    const postValidator = t.Object({                     │
│      title: t.String(),                                 │
│      slug: t.String(),                                  │
│      content: t.String(),                               │
│      description: t.Optional(t.String())                │
│    })                                                   │
│    Result: ✅ VALID (All required fields present)       │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│    DTO VALIDATION (CreatePostDTO)                       │
│    interface CreatePostDTO {                            │
│      title: string;          ✅ "My Post"               │
│      slug: string;           ✅ "my-post"               │
│      content: string;        ✅ "Content..."            │
│      description?: string;   ✅ optional (undefined)    │
│      status?: string;        ✅ optional (undefined)    │
│    }                                                    │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│    SERVICE VALIDATION (Business Logic)                  │
│    - Check slug uniqueness: await findBySlug(slug)      │
│    - Validate content length: content.length > 10       │
│    - Set defaults: status = 'draft'                     │
│    Result: ✅ VALID (All business rules pass)           │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│    REPOSITORY OPERATION (Type-Safe ORM)                 │
│    const post = await PostRepository.create({           │
│      title: string      ✅ Type checked                 │
│      slug: string       ✅ Type checked (unique index)  │
│      content: string    ✅ Type checked                 │
│      status: string     ✅ Type checked (enum-like)     │
│    })                                                   │
│    Result: Post Entity (with id, timestamps)            │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│    DATABASE OPERATION (TypeORM)                         │
│    INSERT INTO posts (                                  │
│      id, title, slug, content, status,                  │
│      createdAt, updatedAt, deletedAt, viewCount         │
│    ) VALUES (                                           │
│      uuid, 'My Post', 'my-post', 'Content...',         │
│      'draft', NOW(), NOW(), NULL, 0                     │
│    )                                                    │
│    Result: ✅ Row inserted with all constraints met     │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│    RESPONSE DTO CONVERSION (PostResponseDTO)            │
│    {                                                    │
│      id: "uuid-string",      ✅ Type checked            │
│      title: "My Post",        ✅ Type checked           │
│      slug: "my-post",         ✅ Type checked           │
│      content: "Content...",   ✅ Type checked           │
│      description: null,       ✅ Type checked           │
│      status: "draft",         ✅ Type checked           │
│      viewCount: 0,            ✅ Type checked           │
│      createdAt: "2024-01-01T00:00:00Z",  ✅ ISO format  │
│      updatedAt: "2024-01-01T00:00:00Z",  ✅ ISO format  │
│      deletedAt: null          ✅ Type checked           │
│    }                                                    │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│    HTTP RESPONSE (Status 201 Created)                   │
│    {                                                    │
│      "status": 201,                                     │
│      "message": "Post created successfully",            │
│      "data": { ... },  ✅ PostResponseDTO               │
│      "timestamp": "2024-01-01T00:00:00Z"                │
│    }                                                    │
└─────────────────────────────────────────────────────────┘
```

---

## Middleware Chain

```
┌─────────────────────────────────────────────────────┐
│  HTTP REQUEST ARRIVES                               │
└────────────────┬────────────────────────────────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ Logger Middleware  │
        │ - Log request      │
        │ - Record timestamp │
        │ - Extract method   │
        └────────┬───────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ CORS Middleware    │
        │ - Check origin     │
        │ - Validate headers │
        │ - Set CORS headers │
        └────────┬───────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ Error Handler      │
        │ (Wraps route)      │
        │ - Try-catch block  │
        │ - Format errors    │
        │ - Return 5xx       │
        └────────┬───────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ Route Handler      │
        │ - Execute business │
        │   logic            │
        │ - Return response  │
        └────────┬───────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ Logger Middleware  │
        │ - Log response     │
        │ - Record duration  │
        │ - Log status code  │
        └────────┬───────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────┐
│  HTTP RESPONSE SENT                                 │
└─────────────────────────────────────────────────────┘
```

---

## Database Connection Pool

```
┌──────────────────────────────────────────────────┐
│  TypeORM DataSource                              │
│  ┌──────────────────────────────────────────────┐│
│  │  Connection Pooling (default: 10 connections)││
│  │  ┌────────────────────────────────────────┐ ││
│  │  │ Database Connection 1                  │ ││
│  │  │ ├─ Query 1                             │ ││
│  │  │ ├─ Query 2                             │ ││
│  │  │ └─ [Idle]                              │ ││
│  │  ├────────────────────────────────────────┤ ││
│  │  │ Database Connection 2                  │ ││
│  │  │ ├─ Query 1                             │ ││
│  │  │ └─ [Waiting]                           │ ││
│  │  ├────────────────────────────────────────┤ ││
│  │  │ Database Connection 3-10               │ ││
│  │  │ └─ [Idle]                              │ ││
│  │  └────────────────────────────────────────┘ ││
│  └──────────────────────────────────────────────┘│
│  Repositories (UserRepository, PostRepository)   │
│  └─ Use connection pool for queries              │
└──────────────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────┐
│  PostgreSQL Server (Containerized)               │
│  ├─ users table                                  │
│  ├─ posts table                                  │
│  └─ Shared buffer pool (256MB)                   │
└──────────────────────────────────────────────────┘
```

---

## Deployment Architecture

```
┌─────────────────────────────────────────────────┐
│  Docker Image (Multi-stage build)               │
│  ┌───────────────────────────────────────────┐  │
│  │ Stage 1: Builder                          │  │
│  │ - Install dependencies (bun install)      │  │
│  │ - Compile TypeScript (bun build)          │  │
│  │ - Size: ~800MB (intermediate)             │  │
│  └───────────────────────────────────────────┘  │
│                      │                          │
│                      ▼                          │
│  ┌───────────────────────────────────────────┐  │
│  │ Stage 2: Production                       │  │
│  │ - Copy compiled code                      │  │
│  │ - Copy node_modules (production only)     │  │
│  │ - Set entry point: bun run start          │  │
│  │ - Size: ~350MB (optimized)                │  │
│  └───────────────────────────────────────────┘  │
│  │                                            │
│  └─ Exposed Port: 3000                        │
└─────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────┐
│  docker-compose.yml                             │
│  ┌─────────────────────────────┐               │
│  │  Service: app               │               │
│  │  ├─ Image: built-image      │               │
│  │  ├─ Ports: 3000:3000        │               │
│  │  ├─ Env: NODE_ENV=prod      │               │
│  │  └─ depends_on: db          │               │
│  ├─────────────────────────────┤               │
│  │  Service: db (PostgreSQL)   │               │
│  │  ├─ Image: postgres:16      │               │
│  │  ├─ Ports: 5432:5432        │               │
│  │  ├─ Volume: postgres_data   │               │
│  │  └─ Health check: pg_isready│               │
│  └─────────────────────────────┘               │
└─────────────────────────────────────────────────┘
```

---

## Summary

✅ **7-Layer Architecture** enables clean separation of concerns
✅ **Type Safety** from request to database response
✅ **Middleware Chain** handles cross-cutting concerns
✅ **Connection Pooling** optimizes database performance
✅ **Docker Support** enables containerized deployment
✅ **Extensible Design** allows adding new modules without changes

This architecture is **production-ready** and **infinitely scalable**.
